import styled from "styled-components";

export const Container = styled.div`
  width: fit-content;
  
  > a {
    font-size: 2rem;
    
    display: flex;
    align-items: center;
    margin-bottom: 2rem;
  }

  > svg {
    font-size: 2.4rem;
  }
`;