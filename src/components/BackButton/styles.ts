import styled from "styled-components";

export const Container = styled.div`
  width: fit-content;
  
  > a {
    font-size: 1.6rem;
    
    display: flex;
    align-items: center;
    margin-bottom: 2rem;
  }

  > svg {
    font-size: 2.4rem;
  }
`;